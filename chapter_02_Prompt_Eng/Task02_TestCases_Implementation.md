# VWO Login Page — Test Case Suite

**Target URL**: https://app.vwo.com/#/login
**Generated**: 2026-08-08
**Total Test Cases**: 60
**Traceability**: Derived from VWO-LOGIN-TP-001 (Task01_TestPlan_Implementation.md)

---

## Category 1: Functional — Happy Path (6 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-FUNC-001 | Functional — Happy Path | Login with valid email and password | User has a registered VWO account; user is on the login page | 1. Enter valid email in the email field. 2. Enter valid password in the password field. 3. Click the "Sign In" button. | Email: `testuser@vwo.com`; Password: `ValidP@ss123` | User is redirected to the VWO dashboard; session token is stored; no error message displayed | P0 — Critical | Y |
| TC-FUNC-002 | Functional — Happy Path | Login via Google SSO with authorized Google account | Google account is linked to a VWO account; user is on the login page | 1. Click "Sign in with Google" button. 2. On Google consent screen, select the authorized Google account. 3. Grant requested permissions. | Google account: `vwo.test@gmail.com` | User is redirected to VWO dashboard; OAuth token exchange successful; user session established | P0 — Critical | Y |
| TC-FUNC-003 | Functional — Happy Path | Login via Microsoft SSO with authorized Microsoft account | Microsoft Azure AD account is linked to a VWO account; user is on the login page | 1. Click "Sign in with Microsoft" button. 2. On Microsoft login screen, enter Microsoft credentials. 3. Grant requested permissions if prompted. | Microsoft account: `vwo.test@outlook.com` | User is redirected to VWO dashboard; Azure AD token exchange successful; user session established | P0 — Critical | Y |
| TC-FUNC-004 | Functional — Happy Path | Login with "Remember Me" enabled — session persists across browser restart | User has a registered VWO account; user is on the login page | 1. Check the "Remember Me" checkbox. 2. Enter valid email and password. 3. Click "Sign In". 4. Close the browser completely. 5. Reopen browser and navigate to `app.vwo.com`. | Email: `testuser@vwo.com`; Password: `ValidP@ss123` | User remains authenticated; is redirected directly to dashboard without re-entering credentials; persistent session cookie present | P1 — High | Y |
| TC-FUNC-005 | Functional — Happy Path | Login with email containing uppercase characters | User has a registered VWO account with lowercase email; user is on the login page | 1. Enter the registered email with mixed case (e.g., TestUser@VWO.com). 2. Enter valid password. 3. Click "Sign In". | Email: `TESTUSER@VWO.COM`; Password: `ValidP@ss123` | Login is successful; email case-insensitivity handled correctly; user redirected to dashboard | P2 — Medium | Y |
| TC-FUNC-006 | Functional — Happy Path | "Create Account" / "Sign Up" link navigates correctly | User is on the login page and does not have an account | 1. Locate the "Create Account" or "Sign Up" link. 2. Click the link. | N/A | User is redirected to the VWO registration/sign-up page; URL changes from `/login` to registration path | P3 — Low | Y |

---

## Category 2: Functional — Negative (10 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-NEG-001 | Functional — Negative | Login with incorrect password for a registered email | User has a registered VWO account; user is on the login page | 1. Enter a valid registered email. 2. Enter an incorrect password. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `WrongP@ss999` | Appropriate error message is displayed (e.g., "Invalid credentials" or equivalent); user remains on login page; no session token issued | P0 — Critical | Y |
| TC-NEG-002 | Functional — Negative | Login with an unregistered email address | User is on the login page; email is not registered with VWO | 1. Enter an email not registered in VWO. 2. Enter any password. 3. Click "Sign In". | Email: `unregistered@example.com`; Password: `SomeP@ss123` | Appropriate error message is displayed; user remains on login page; no account enumeration vulnerability (message should not reveal if email exists) | P0 — Critical | Y |
| TC-NEG-003 | Functional — Negative | Submit login form with empty email field | User is on the login page | 1. Leave the email field empty. 2. Enter a valid password. 3. Click "Sign In". | Email: (empty); Password: `ValidP@ss123` | Client-side validation prevents submission OR appropriate field-level error is displayed (e.g., "Email is required"); form is not submitted to the server | P1 — High | Y |
| TC-NEG-004 | Functional — Negative | Submit login form with empty password field | User is on the login page | 1. Enter a valid email. 2. Leave the password field empty. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: (empty) | Client-side validation prevents submission OR appropriate field-level error is displayed (e.g., "Password is required"); form is not submitted to the server | P1 — High | Y |
| TC-NEG-005 | Functional — Negative | Submit login form with both fields empty | User is on the login page | 1. Leave both email and password fields empty. 2. Click "Sign In". | Email: (empty); Password: (empty) | Client-side validation prevents submission; appropriate error messages displayed for both fields | P1 — High | Y |
| TC-NEG-006 | Functional — Negative | Login with whitespace-only email field | User is on the login page | 1. Enter one or more spaces in the email field. 2. Enter a valid password. 3. Click "Sign In". | Email: `   ` (3 spaces); Password: `ValidP@ss123` | System rejects the input; appropriate error message displayed; whitespace is not treated as a valid email | P2 — Medium | Y |
| TC-NEG-007 | Functional — Negative | Login with whitespace-only password field | User is on the login page | 1. Enter a valid email. 2. Enter one or more spaces in the password field. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `   ` (3 spaces) | System rejects the input; appropriate error message displayed; whitespace is not treated as a valid password | P2 — Medium | Y |
| TC-NEG-008 | Functional — Negative | Google SSO — user denies consent on Google consent screen | User is on the login page | 1. Click "Sign in with Google". 2. On the Google consent screen, click "Cancel" or "Deny". | Google account: any valid Google account | User is returned to the VWO login page; no authentication occurs; appropriate message or silent redirect back to login; no error page displayed | P1 — High | N |
| TC-NEG-009 | Functional — Negative | Microsoft SSO — user cancels authentication on Microsoft login | User is on the login page | 1. Click "Sign in with Microsoft". 2. On the Microsoft login page, click "Cancel" or close the window. | Microsoft account: any valid Microsoft account | User is returned to the VWO login page; no authentication occurs; no session established; login page state preserved | P1 — High | N |
| TC-NEG-010 | Functional — Negative | Login with an email missing the @ symbol | User is on the login page | 1. Enter an email string without "@" (e.g., testuservwo.com). 2. Enter any password. 3. Click "Sign In". | Email: `testuservwo.com`; Password: `SomeP@ss123` | Client-side or server-side validation rejects the malformed email; appropriate error message displayed | P2 — Medium | Y |

---

## Category 3: Boundary Testing (7 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-BND-001 | Boundary | Email at minimum acceptable length (e.g., a@b.co — 6 chars) | User is on the login page | 1. Enter an extremely short but valid email (a@b.co). 2. Enter a valid password. 3. Click "Sign In". | Email: `a@b.co`; Password: `ValidP@ss123` | System accepts the short email format; either login proceeds (if registered) or appropriate invalid-credentials error is displayed | P2 — Medium | Y |
| TC-BND-002 | Boundary | Email at maximum acceptable length (320 characters per RFC 5321) | User is on the login page | 1. Enter an email with 320 total characters (local@domain). 2. Enter a valid password. 3. Click "Sign In". | Email: 320-char valid-format email string | System accepts the email without truncation or buffer overflow; appropriate response returned (registered or not) | P2 — Medium | Y |
| TC-BND-003 | Boundary | Email exceeding maximum length (321+ characters) | User is on the login page | 1. Enter an email exceeding 320 characters. 2. Enter any password. 3. Click "Sign In". | Email: 400-char string (valid format but too long) | System rejects the input; appropriate error message OR graceful truncation; no crash or 500 error | P2 — Medium | Y |
| TC-BND-004 | Boundary | Password at minimum length boundary (e.g., 8 characters if that is the policy) | User has an account with minimum-length password; user is on the login page | 1. Enter a valid email. 2. Enter a password of exactly the minimum required length. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `Abcd1234` (8 chars) | Login proceeds normally if credentials are correct; no truncation or minimum-length error for a valid-length password | P2 — Medium | Y |
| TC-BND-005 | Boundary | Password at maximum acceptable length (e.g., 128 characters) | User is on the login page | 1. Enter a valid email. 2. Enter a 128-character password. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: 128-char alphanumeric string | System accepts the password without truncation; appropriate response (success or invalid credentials); no buffer overflow | P2 — Medium | Y |
| TC-BND-006 | Boundary | Password containing special characters only | User is on the login page | 1. Enter a valid email. 2. Enter a password made entirely of special characters. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `!@#$%^&*()_+-={}[]|:;"'<>,.?/~` | System accepts special characters without breaking; appropriate response returned; no encoding errors | P2 — Medium | Y |
| TC-BND-007 | Boundary | Email and password fields with Unicode and emoji characters | User is on the login page | 1. Enter email containing Unicode characters. 2. Enter password containing emoji. 3. Click "Sign In". | Email: `tést@vwo.com` (with é); Password: `Pass🔐123` (with lock emoji) | System handles Unicode/emoji gracefully; either processes normally or rejects with clear error; no crash, garbled text, or encoding errors | P3 — Low | Y |

---

## Category 4: Security (12 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-SEC-001 | Security | Brute-force protection — account lockout after N consecutive failed attempts | User is on the login page; account lockout policy is configured (e.g., 5 attempts) | 1. Attempt login with incorrect password 5+ times consecutively for the same email. 2. On the 6th attempt, enter an incorrect password again. | Email: `testuser@vwo.com`; Password: `WrongP@ss` each time | After exceeding the threshold, the account is temporarily locked; appropriate lockout message is displayed (e.g., "Account temporarily locked. Try again in X minutes"); further login attempts blocked regardless of password correctness | P1 — High | N |
| TC-SEC-002 | Security | CAPTCHA triggered after multiple failed login attempts | User is on the login page; CAPTCHA is configured to appear after N failed attempts | 1. Attempt login with incorrect credentials repeatedly until CAPTCHA threshold is reached. 2. Observe the login page. | Email: `testuser@vwo.com`; Password: `WrongP@ss` repeated | A CAPTCHA challenge is displayed after the threshold; login form submission is blocked until CAPTCHA is successfully completed | P1 — High | N |
| TC-SEC-003 | Security | SQL injection in email field | User is on the login page | 1. Enter a SQL injection payload in the email field. 2. Enter any password. 3. Click "Sign In". | Email: `' OR '1'='1' --`; Password: `anything` | System sanitizes the input; does not execute SQL injection; returns a generic error (e.g., "Invalid credentials"); no database error or data leakage | P1 — High | Y |
| TC-SEC-004 | Security | SQL injection in password field | User is on the login page | 1. Enter a valid email. 2. Enter a SQL injection payload in the password field. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `' OR '1'='1` | System sanitizes the input; does not execute SQL injection; returns a generic error; no database error or data leakage | P1 — High | Y |
| TC-SEC-005 | Security | XSS — script injection in email field | User is on the login page | 1. Enter an XSS payload in the email field. 2. Click "Sign In" or trigger validation. | Email: `<script>alert('XSS')</script>`; Password: `anything` | The script is not executed in the browser; the payload is sanitized, HTML-encoded, or rejected; no alert dialog appears; error message does not render the script | P1 — High | Y |
| TC-SEC-006 | Security | XSS — script injection in password field | User is on the login page | 1. Enter a valid email. 2. Enter an XSS payload in the password field. 3. Click "Sign In". | Email: `testuser@vwo.com`; Password: `<img src=x onerror=alert(1)>` | The script is not executed; payload is sanitized or rejected; no alert or unexpected behavior | P1 — High | Y |
| TC-SEC-007 | Security | HTTPS enforced — HTTP request redirects to HTTPS | User is on the login page | 1. Navigate to `http://app.vwo.com/#/login` (HTTP). | URL: `http://app.vwo.com/#/login` | Browser is automatically redirected to `https://app.vwo.com/#/login`; all resources loaded over HTTPS; HSTS header present if configured | P1 — High | Y |
| TC-SEC-008 | Security | Password field masks input characters | User is on the login page | 1. Click into the password field. 2. Type a password. 3. Observe the field visually. | Password: `MySecretP@ss123` | Password characters are masked (displayed as dots or asterisks); password is not visible in plain text; no toggle-to-show feature reveals password without explicit user action | P2 — Medium | Y |
| TC-SEC-009 | Security | CSRF token validation on login form submission | User is on the login page | 1. Inspect the login form HTML for a CSRF token (hidden input or header). 2. Submit the form with valid credentials. 3. Repeat submission with a modified/missing CSRF token (via browser dev tools). | Valid credentials | Form submission includes a CSRF token; server validates the token; submission with missing/invalid token is rejected with an appropriate error | P1 — High | N |
| TC-SEC-010 | Security | Session fixation — pre-set session ID not reused after login | User is on the login page (unauthenticated); a session cookie is already present | 1. Note the session cookie value before login (if any). 2. Perform a successful login. 3. Inspect the session cookie after login. | Valid credentials | A new session ID/token is issued after successful authentication; the pre-login session ID is invalidated; old session ID cannot be used to access authenticated resources | P1 — High | Y |
| TC-SEC-011 | Security | Autocomplete disabled on password field (or set to "off") | User is on the login page | 1. Inspect the password field's `autocomplete` attribute via browser dev tools. 2. Log in successfully. 3. Log out. 4. Return to the login page and check if the password is auto-filled. | N/A | The password field has `autocomplete="off"` or `autocomplete="new-password"` set; browser does not auto-save or auto-fill the password unless explicitly configured by the user | P2 — Medium | Y |
| TC-SEC-012 | Security | Copy and paste into password field | User is on the login page | 1. Copy a string to the clipboard. 2. Paste it into the password field. 3. Attempt to copy the masked password from the field. | Password: `MySecureP@ss` | Pasting INTO the password field is allowed (usability); copying FROM the password field is prevented or returns masked content; field remains masked during paste | P3 — Low | Y |

---

## Category 5: Session Management (6 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-SESS-001 | Session Management | Active session persists with "Remember Me" after browser restart | User logged in with "Remember Me" checked; browser closed | 1. Log in with "Remember Me" enabled. 2. Close the browser completely. 3. After 5 minutes, reopen browser and navigate to `app.vwo.com`. | Valid credentials with Remember Me | User is automatically authenticated; redirected to dashboard without login prompt; persistent cookie/token remains valid | P1 — High | Y |
| TC-SESS-002 | Session Management | Session expires after idle timeout (no Remember Me) | User logged in WITHOUT "Remember Me" | 1. Log in without checking "Remember Me". 2. Remain idle for the configured session timeout period (e.g., 30 minutes). 3. Click any navigation element or refresh the page. | Valid credentials | User is redirected to the login page; session token is expired; any action requiring authentication returns to login; appropriate message or silent redirect | P1 — High | N |
| TC-SESS-003 | Session Management | Logout invalidates session — browser back button does not restore authenticated state | User logged in and then logged out | 1. Log in successfully. 2. Navigate to a page within the app. 3. Click "Logout". 4. After logout, press the browser back button multiple times. | Valid credentials | User remains on the login page or is redirected to login; cached authenticated pages are not accessible; session token is invalidated server-side | P1 — High | Y |
| TC-SESS-004 | Session Management | Concurrent login from two different browsers | User has an active session in Browser A | 1. Log in from Browser A (Chrome). 2. Open Browser B (Firefox) and log in with the same credentials. 3. Return to Browser A and perform an action. | Same valid credentials in both browsers | Both sessions remain active OR the older session is terminated based on VWO's concurrency policy; appropriate behavior without data corruption; clear messaging if session is terminated | P2 — Medium | Y |
| TC-SESS-005 | Session Management | Concurrent login from two different tabs in the same browser | User has an active session in Tab A | 1. Log in from Tab A. 2. Open Tab B and navigate directly to the login page. 3. Observe Tab B's behavior. | Same browser, same credentials | Tab B either: (a) auto-redirects to dashboard (session already active in browser), or (b) shows login page but submitting redirects without re-authentication; state is consistent across tabs | P2 — Medium | Y |
| TC-SESS-006 | Session Management | Token expiry — session is not valid after token expiration | User has a session with a known expiry time | 1. Log in and note the session token. 2. Wait for the token to expire (or manually expire it). 3. Attempt to access a protected resource with the expired token. | Expired session token | Access is denied; user is redirected to the login page; appropriate error message or redirect; no access to protected resources with expired token | P1 — High | Y |

---

## Category 6: Forgot Password Flow (8 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-FP-001 | Forgot Password | Forgot Password link visible and clickable on login page | User is on the login page | 1. Locate the "Forgot Password?" link on the login page. 2. Verify the link is visible and not broken. 3. Click the link. | N/A | "Forgot Password?" link is visible and functional; user is navigated to the password recovery page/form | P1 — High | Y |
| TC-FP-002 | Forgot Password | Submit valid registered email for password reset | User is on the Forgot Password page | 1. Enter a valid registered email address. 2. Click "Submit" or "Send Reset Link". | Email: `testuser@vwo.com` | Success message is displayed (e.g., "If an account with this email exists, a reset link has been sent"); a password reset email is sent to the registered email inbox | P1 — High | Y |
| TC-FP-003 | Forgot Password | Submit unregistered email — generic success message (no enumeration) | User is on the Forgot Password page | 1. Enter an email NOT registered with VWO. 2. Click "Submit" or "Send Reset Link". | Email: `unregistered@example.com` | Same generic success message is displayed as for a registered email (e.g., "If an account exists, a reset link has been sent"); no indication of whether the email is registered; prevents account enumeration | P1 — High | Y |
| TC-FP-004 | Forgot Password | Submit empty email on Forgot Password form | User is on the Forgot Password page | 1. Leave the email field empty. 2. Click "Submit". | Email: (empty) | Client-side or server-side validation prevents submission; error message displayed (e.g., "Email is required"); no request sent to server | P2 — Medium | Y |
| TC-FP-005 | Forgot Password | Submit invalid email format on Forgot Password form | User is on the Forgot Password page | 1. Enter a malformed email (e.g., "notanemail"). 2. Click "Submit". | Email: `notanemail` (no @) | Validation error displayed; "Please enter a valid email address" or equivalent; form is not submitted | P2 — Medium | Y |
| TC-FP-006 | Forgot Password | Password reset link is functional and allows setting a new password | User received a valid password reset email | 1. Open the password reset email. 2. Click the reset link. 3. Enter a new password (meeting complexity requirements). 4. Confirm the new password. 5. Click "Reset Password". | New Password: `NewSecureP@ss456` | Password reset is successful; success message displayed; user is redirected to the login page; user can now log in with the new password | P1 — High | N |
| TC-FP-007 | Forgot Password | Password reset link has expired | A password reset link was generated more than the expiry window ago (e.g., 1 hour) | 1. Click an expired password reset link. | Expired reset token | Appropriate error message displayed (e.g., "This reset link has expired"); no password reset occurs; user is prompted to request a new link | P1 — High | Y |
| TC-FP-008 | Forgot Password | After successful password reset, old password no longer works | User has reset their password to a new value | 1. Complete the password reset flow with a new password. 2. Return to the login page. 3. Attempt to log in with the OLD password. | Old Password: `OldP@ss123`; New Password: `NewSecureP@ss456` | Login with old password fails; appropriate error message displayed; only the new password grants access | P1 — High | Y |

---

## Category 7: Usability (5 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-USB-001 | Usability | Tab order follows logical navigation sequence | User is on the login page | 1. Press the Tab key repeatedly and observe focus order. | N/A | Focus moves in logical order: Email → Password → Sign In button → Forgot Password link → Google SSO button → Microsoft SSO button → Create Account link → (cycle back); focus indicator is clearly visible on each element | P2 — Medium | Y |
| TC-USB-002 | Usability | Pressing Enter key on email field moves focus to password field | User is on the login page; cursor is in the email field | 1. Type an email in the email field. 2. Press the Enter key. | Email: `testuser@vwo.com` | Focus moves to the password field; form is NOT submitted; intuitive keyboard navigation behavior | P2 — Medium | Y |
| TC-USB-003 | Usability | Pressing Enter key on password field submits the login form | User is on the login page; email and password are filled | 1. Fill email field. 2. Fill password field. 3. With cursor in password field, press Enter. | Valid credentials | Login form is submitted; same behavior as clicking the "Sign In" button; no duplicate submission | P2 — Medium | Y |
| TC-USB-004 | Usability | Error message is clear, human-readable, and actionable | User is on the login page | 1. Attempt login with invalid credentials. 2. Read the error message displayed. | Invalid credentials | Error message is: (a) displayed near the form, (b) written in plain language, (c) indicates what went wrong without revealing sensitive information, (d) visually distinct (e.g., red color, icon) | P2 — Medium | N |
| TC-USB-005 | Usability | Loading indicator displayed while authentication request is in progress | User is on the login page; simulate a slow network | 1. Enable network throttling (Slow 3G in DevTools). 2. Enter credentials and click "Sign In". 3. Observe the UI during the request. | Valid credentials | A loading indicator (spinner, progress bar, or disabled button with "Signing in…" text) is displayed; the button is disabled to prevent double-click; UI is not frozen | P2 — Medium | Y |

---

## Category 8: Compatibility (6 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-CMP-001 | Compatibility | Login page renders and functions correctly on Chrome (latest version) | Chrome browser (latest) installed | 1. Open Chrome. 2. Navigate to `https://app.vwo.com/#/login`. 3. Verify all elements and perform a login attempt. | Chrome latest (126–127) | All UI elements render correctly; no layout issues; login flow works end-to-end | P1 — High | Y |
| TC-CMP-002 | Compatibility | Login page renders and functions correctly on Firefox (latest version) | Firefox browser (latest) installed | 1. Open Firefox. 2. Navigate to the login page. 3. Verify all elements and attempt login. | Firefox latest (128–129) | All UI elements render correctly; no layout issues; login flow works end-to-end | P1 — High | Y |
| TC-CMP-003 | Compatibility | Login page renders and functions correctly on Safari (latest version) | Safari browser (latest) on macOS | 1. Open Safari. 2. Navigate to the login page. 3. Verify all elements and attempt login. | Safari latest (17–18) | All UI elements render correctly; no layout issues; SSO redirects work with Safari's ITP (Intelligent Tracking Prevention); login flow works end-to-end | P1 — High | Y |
| TC-CMP-004 | Compatibility | Login page renders and functions correctly on Edge (latest version) | Edge browser (latest) installed | 1. Open Edge. 2. Navigate to the login page. 3. Verify all elements and attempt login. | Edge latest (126–127) | All UI elements render correctly; no layout issues; login flow works end-to-end | P1 — High | Y |
| TC-CMP-005 | Compatibility | Login page renders correctly on mobile viewport — iOS Safari | iPhone or iOS Simulator with Safari | 1. Open Safari on iOS. 2. Navigate to the login page. 3. Observe layout at 390px width (iPhone 14/15). | iOS Safari 17–18 | All elements are visible and usable; no horizontal scrolling required; buttons are tap-target sized (≥44px); form fields are appropriately sized; SSO buttons work | P1 — High | Y |
| TC-CMP-006 | Compatibility | Login page renders correctly on mobile viewport — Android Chrome | Android device or emulator with Chrome | 1. Open Chrome on Android. 2. Navigate to the login page. 3. Observe layout at 360–412px width. | Android Chrome 126–127 | All elements are visible and usable; no horizontal scrolling; buttons are tap-target sized; form fields appropriately sized; SSO buttons work | P1 — High | Y |

---

## Category 9: Accessibility (5 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-A11Y-001 | Accessibility | All form elements have associated labels (WCAG 1.3.1) | User is on the login page | 1. Inspect HTML of email and password fields. 2. Verify they have associated `<label>` elements or `aria-label` attributes. | N/A | Email and password fields have properly associated labels using `<label for="...">` or `aria-label`; screen reader announces the label when field is focused | P2 — Medium | Y |
| TC-A11Y-002 | Accessibility | Login page is fully navigable using keyboard only (WCAG 2.1.1) | User is on the login page; mouse is disconnected/disabled | 1. Use Tab/Shift+Tab to navigate all interactive elements. 2. Use Enter/Space to activate buttons and links. 3. Use Escape to close any modals. | N/A | All interactive elements (fields, buttons, links, SSO buttons, checkbox) are reachable and operable via keyboard alone; focus indicator is visible on every element; no keyboard traps | P2 — Medium | N |
| TC-A11Y-003 | Accessibility | Color contrast meets WCAG AA minimum (4.5:1 for text, 3:1 for large text) | User is on the login page | 1. Use an accessibility tool (axe-core, Lighthouse, WAVE) to audit color contrast. 2. Manually verify text over background colors. | N/A | All text elements meet or exceed 4.5:1 contrast ratio; large text (≥18px bold or ≥24px) meets 3:1 ratio; error messages, placeholders, and labels all pass | P2 — Medium | Y |
| TC-A11Y-004 | Accessibility | Screen reader announces error messages correctly (WCAG 4.1.3) | Screen reader (NVDA/VoiceOver) is running; user is on the login page | 1. Submit the login form empty. 2. Listen to screen reader output. 3. Submit the login form with invalid credentials. 4. Listen to screen reader output. | NVDA (Windows) or VoiceOver (macOS) | Error messages are programmatically associated with their fields via `aria-describedby`; screen reader announces the error when it appears; errors are descriptive (not just "Error") | P2 — Medium | N |
| TC-A11Y-005 | Accessibility | Focus management — focus is set to the first field on page load | User is on the login page; page freshly loaded | 1. Load the login page without clicking anywhere. 2. Press Tab once and observe the focused element. | N/A | On page load, focus is set to the email input field (or the first actionable element); the focus ring/outline is clearly visible; no unexpected focus jumps | P3 — Low | Y |

---

## Category 10: Performance (6 test cases)

| TC-ID | Category | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Priority | Automation Ready (Y/N) |
|---|---|---|---|---|---|---|---|---|
| TC-PERF-001 | Performance | Login page loads within 3 seconds (P95) on standard broadband | User is on a standard broadband connection (~10 Mbps) | 1. Open Chrome DevTools → Network tab. 2. Clear cache (hard reload). 3. Navigate to `https://app.vwo.com/#/login`. 4. Record page load time (Load event / FCP / LCP). | Standard broadband | Page loads within 3 seconds (First Contentful Paint ≤ 1.5s, Largest Contentful Paint ≤ 2.5s, Time to Interactive ≤ 3s); no render-blocking resources causing excessive delay | P1 — High | Y |
| TC-PERF-002 | Performance | Authentication API responds within 2 seconds | User is on the login page | 1. Open Chrome DevTools → Network tab. 2. Enter valid credentials and click "Sign In". 3. Measure the authentication API request duration. | Valid credentials | Authentication API response time ≤ 2 seconds (P95); network waterfall shows the auth request completes promptly; no timeout errors | P1 — High | Y |
| TC-PERF-003 | Performance | Login page loads within 5 seconds on 3G connection | User is on a slow 3G connection (~1.6 Mbps, 300ms RTT) | 1. Enable network throttling to "Slow 3G" in Chrome DevTools. 2. Navigate to `https://app.vwo.com/#/login`. 3. Record page load time. | Slow 3G throttling | Page becomes interactive within 5–7 seconds; critical content (login form) renders first; user is not staring at a blank white screen | P2 — Medium | Y |
| TC-PERF-004 | Performance | Concurrent login load — 50 users attempting login simultaneously | Load testing tool (k6/JMeter) configured with 50 virtual users | 1. Configure k6 script with 50 VUs. 2. Ramp up over 30 seconds. 3. Each VU submits valid credentials. 4. Monitor response times and error rates. | 50 concurrent virtual users with valid credentials | ≥ 95% of requests succeed; average response time ≤ 3s under load; no 5xx errors; no cascading failures or deadlocks; system recovers after load subsides | P1 — High | Y |
| TC-PERF-005 | Performance | CSS/JS resources are properly cached on repeat visits | User has visited the login page once | 1. First visit: navigate to the login page and note resources loaded. 2. Second visit: reload the page without clearing cache. 3. Compare network activity. | N/A | On repeat visit, static assets (CSS, JS, fonts, images) are served from disk/memory cache (HTTP 304 or from cache); page load time on repeat visit is significantly faster; appropriate cache headers (Cache-Control, ETag) are set | P2 — Medium | Y |
| TC-PERF-006 | Performance | No JavaScript errors in the console during login flow | User is on the login page | 1. Open Chrome DevTools → Console tab. 2. Perform a full login flow (valid credentials). 3. Observe console for any errors or warnings. | Valid credentials | Zero JavaScript errors in console; any warnings are non-blocking (e.g., deprecation notices only); uncaught exceptions = test failure | P1 — High | Y |

---

## Summary Statistics

| Category | Count | P0 — Critical | P1 — High | P2 — Medium | P3 — Low | Automation Ready |
|---|---|---|---|---|---|---|
| 1. Functional — Happy Path | 6 | 3 | 1 | 1 | 1 | 6 |
| 2. Functional — Negative | 10 | 2 | 5 | 3 | 0 | 8 |
| 3. Boundary | 7 | 0 | 0 | 5 | 2 | 6 |
| 4. Security | 12 | 0 | 8 | 3 | 1 | 8 |
| 5. Session Management | 6 | 0 | 3 | 3 | 0 | 4 |
| 6. Forgot Password | 8 | 0 | 5 | 3 | 0 | 6 |
| 7. Usability | 5 | 0 | 0 | 5 | 0 | 3 |
| 8. Compatibility | 6 | 0 | 6 | 0 | 0 | 6 |
| 9. Accessibility | 5 | 0 | 0 | 4 | 1 | 3 |
| 10. Performance | 6 | 0 | 4 | 2 | 0 | 6 |
| **TOTAL** | **60** | **5** | **32** | **29** | **5** | **56 / 60 (93%)** |

---

**Document Status**: ✅ COMPLETE

**Traceability**: All test cases are mapped to requirements defined in VWO-LOGIN-TP-001 (Task01_TestPlan_Implementation.md). Each TC-ID corresponds to a Feature ID in the Test Plan's Traceability Matrix.